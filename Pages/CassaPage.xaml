<Page x:Class="CazinoProjectSlepPas.Pages.CassaPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:cazinoprojectsleppas="clr-namespace:CazinoProjectSlepPas" d:DataContext="{d:DesignInstance Type=cazinoprojectsleppas:AccountClass}"
      mc:Ignorable="d" 
      d:DesignHeight="830" d:DesignWidth="1566"
      Title="Касса">

    <Page.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#FF42469E"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBox" x:Key="ModernTextBox">
            <Setter Property="Background" Value="#FF2D2D4D"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                BorderBrush="#FF6A6A9E"
                                BorderThickness="1">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Общий стиль для анимированных кнопок -->
        <Style x:Key="AnimatedButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4C4C7A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}" 
                                CornerRadius="5"
                                Padding="10,5">
                            <Border.RenderTransform>
                                <ScaleTransform x:Name="buttonScale" ScaleX="1" ScaleY="1"/>
                            </Border.RenderTransform>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <!-- Анимация нажатия -->
                            <EventTrigger RoutedEvent="Button.PreviewMouseDown">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="buttonScale"
                                                     Storyboard.TargetProperty="ScaleX"
                                                     To="0.95" Duration="0:0:0.05"/>
                                        <DoubleAnimation Storyboard.TargetName="buttonScale"
                                                     Storyboard.TargetProperty="ScaleY"
                                                     To="0.95" Duration="0:0:0.05"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="Button.PreviewMouseUp">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="buttonScale"
                                                     Storyboard.TargetProperty="ScaleX"
                                                     To="1" Duration="0:0:0.1"/>
                                        <DoubleAnimation Storyboard.TargetName="buttonScale"
                                                     Storyboard.TargetProperty="ScaleY"
                                                     To="1" Duration="0:0:0.1"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <!-- Эффекты при наведении -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#5C5C8A"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="10" Opacity="0.3" ShadowDepth="2"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid >
        
        <Border CornerRadius="25" Background="#7F4C4C6D" Margin="620,70,620,100">
            <Grid VerticalAlignment="Stretch" HorizontalAlignment="Stretch" >
            
                
                
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <!-- Заголовок -->
                    <RowDefinition Height="30"/>
                    <!-- Отступ -->
                    <RowDefinition Height="Auto"/>
                    <!-- Секция пополнения -->
                    <RowDefinition Height="20"/>
                    <!-- Отступ -->
                    <RowDefinition Height="Auto"/>
                    <!-- Секция снятия -->
                    <RowDefinition Height="20"/>
                    <!-- Отступ -->
                    <RowDefinition Height="Auto"/>
                    <!-- Баланс -->
                    <RowDefinition Height="40"/>
                    <!-- Отступ -->
                    <RowDefinition Height="Auto"/>
                    <!-- Кнопка акций -->
                </Grid.RowDefinitions>

                <!-- Заголовок -->
                <TextBlock Text="КАССА" Grid.Row="0"
                               FontSize="28" FontWeight="Bold" 
                               HorizontalAlignment="Center" Foreground="White" Margin="0,30,0,0"/>

                <!-- Секция пополнения -->
                <Border Grid.Row="2" Background="#2A2A4A" CornerRadius="10" Padding="20" Width="400" HorizontalAlignment="Center">
                    <StackPanel>
                        <TextBlock Text="Пополнение счета" FontSize="18" FontWeight="Bold" Margin="0,0,0,15" Foreground="White"/>
                        <TextBlock Text="Сумма пополнения:" Margin="5" Foreground="White"/>
                        <TextBox x:Name="DepositAmount" Style="{StaticResource ModernTextBox}" ToolTip="Введите сумму"/>
                        <Button Content="ПОЛОЖИТЬ" 
                                    Style="{StaticResource AnimatedButtonStyle}"
                                    Margin="0,20,0,0" 
                                    Height="35"
                                    Click="Deposit_Click" 
                                    HorizontalAlignment="Center" 
                                    Width="160"/>
                    </StackPanel>
                </Border>

                <!-- Секция снятия -->
                <Border Grid.Row="4" Background="#2A2A4A" CornerRadius="10" Padding="20" Width="400" HorizontalAlignment="Center">
                    <StackPanel>
                        <TextBlock Text="Снятие со счета" FontSize="18" FontWeight="Bold" Margin="0,0,0,15" Foreground="White"/>
                        <TextBlock Text="Сумма снятия:" Margin="5" Foreground="White"/>
                        <TextBox x:Name="WithdrawAmount" Style="{StaticResource ModernTextBox}" ToolTip="Введите сумму"/>
                        <Button Content="СНЯТЬ" 
                                    Style="{StaticResource AnimatedButtonStyle}"
                                    Margin="0,20,0,0" 
                                    Height="35"
                                    Click="Withdraw_Click"
                                    HorizontalAlignment="Center" 
                                    Width="160"/>
                    </StackPanel>
                </Border>

                <!-- Текущий баланс -->
                <Border Grid.Row="6" Background="#FF42469E" CornerRadius="10" Padding="15" HorizontalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Текущий баланс:" FontSize="18" FontWeight="Bold" Margin="0,0,15,0" Foreground="White"/>
                        <TextBlock x:Name="CurrentBalance" Text="{Binding NowBalance}" FontSize="18" FontWeight="Bold" Foreground="White" Width="145"/>
                    </StackPanel>
                </Border>

                

            </Grid>
        </Border>  
        
    </Grid>
</Page>